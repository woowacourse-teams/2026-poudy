# 기여 가이드

## 브랜치와 Pull Request

작업 브랜치는 팀 저장소인 `woowacourse-teams/2026-poudy` 에 올리고, 그곳에서 Pull
Request 를 연다. 개인 포크에 올려서 여는 Pull Request 는 GitHub 가 secret 과 variable
을 넘겨주지 않아, Vercel 배포처럼 이 값들이 필요한 검사가 모두 실패한다. 같은 사람이
올렸더라도 저장소가 다르면 값을 받지 못한다.

원격 이름은 클론한 방식에 따라 다르다. 팀 저장소를 그대로 클론했으면 `origin` 이고,
포크를 클론한 뒤 팀 저장소를 더했으면 흔히 `upstream` 이다. 아래 명령으로 어느 이름이
팀 저장소를 가리키는지 확인한 다음 그 이름으로 올린다.

```sh
git remote -v
git push <팀 저장소를 가리키는 이름> <브랜치 이름>
```

## 커밋 메시지 규칙

### 형식

```text
<타입> : <제목>

<본문>

<꼬릿말>
```

- 제목은 `타입 : 제목` 형식으로 작성하고 이모지를 쓰지 않는다.
- 본문은 선택 사항이다. 작성한다면 구체적인 내용을 적고, 여러 줄은 각 줄을 `-`로
  구분한다.
- 꼬릿말은 선택 사항이다. 관련 이슈가 있을 때만 `Ref: #7` 또는 `Close: #7` 형식으로
  작성한다.
- 본문과 꼬릿말은 각각 앞 내용과 빈 줄로 구분한다.

### 타입

| 타입       | 설명                                            |
| ---------- | ----------------------------------------------- |
| `build`    | 빌드 시스템 또는 외부 의존성에 영향을 주는 변경 |
| `ci`       | GitHub Actions 등 CI 설정 파일과 스크립트 변경  |
| `docs`     | 문서만 변경                                     |
| `feat`     | 새로운 기능 추가                                |
| `fix`      | 버그 수정                                       |
| `perf`     | 성능을 개선하는 코드 변경                       |
| `refactor` | 버그 수정이나 기능 추가가 아닌 코드 변경        |
| `test`     | 누락된 테스트 추가 또는 기존 테스트 수정        |

### 커밋 단위

- 모든 커밋은 기능 단위로 나눈다. 여러 기능을 한 커밋에 묶지 않는다.
- 하나의 작업에서 여러 기능이 나오면 각각 별도 커밋으로 분리한다.
- 타입이 섞이는 커밋을 만들지 않는다. 예를 들어 기능 추가와 문서 수정은 `feat` 커밋과
  `docs` 커밋으로 나눈다.

### 머지 커밋

Git이 메시지를 만드는 머지 커밋은 형식을 적용하지 않는다. GitHub이 만드는
`Merge pull request #7 from ...`과 작업 브랜치에서 실행한 `git merge dev`가 해당한다.

기준은 커밋을 실행한 주체가 아니라 메시지를 작성한 주체다. 사람이 메시지를 직접 쓰는
커밋에는 예외가 없다.

### 단독 커밋

- `Co-authored-by` 등 공동 작성자 트레일러를 넣지 않는다.
- `Generated with`와 같은 도구 서명을 넣지 않는다.
- 꼬릿말을 작성한다면 이슈 번호를 나타내는 `Ref:` 또는 `Close:`만 사용한다.

### 예시

```text
feat : 회원 가입 API 추가

- 이메일 중복 검사 로직 구현
- 비밀번호 암호화 적용

Close: #12
```

## 커밋 메시지 템플릿

저장소를 처음 설정할 때 `./setup-git.sh`를 실행하면 `.gitmessage.txt`가 Git 커밋 메시지
템플릿으로 등록된다.

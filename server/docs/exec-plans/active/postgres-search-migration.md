# 상품과 성분의 PostgreSQL 검색 전환

- 상태: 2단계 검색 계약 정리 완료, 3단계 서버 검색 SQL 구현 예정
- 선행 이슈: https://github.com/woowacourse-teams/2026-poudy/issues/480
- 후속 이슈: https://github.com/woowacourse-teams/2026-poudy/issues/492
- 기준 브랜치: `feat/backend-postgres-migration`
- 기준 커밋: `c1afc87`
- 후속 작업 브랜치: `feat/postgres-search-migration`

## 목표와 배경

#480에서 카탈로그의 원천을 PostgreSQL로 옮겼지만 상품과 성분은 기동 시 전체를
메모리에 올려 기존 Java 검색, 필터, 정렬과 집계를 수행한다. DB 검색을 구현하고 관련
조회 경로를 전환해 상품과 성분의 전체 카탈로그 로딩 의존을 제거한다.

조회수, 인기 검색어 집계, 피드백, 정정 요청과 등록 요청의 DB 저장은 기준 브랜치에
구현되어 있다. 이 작업에서는 해당 저장 기능을 다시 구현하지 않는다.

## 확인한 근거와 제약

- #480은 `search_products`, `search_ingredients`로 전환하며 검색 순위 변경을 허용한다.
  상품 목록은 검색을 필터로 사용하고 모든 토큰이 맞는 결과만 남긴다(`p_max_tier = 2`).
- API 요청과 응답 형식, 클라이언트와 모바일 코드는 유지한다. 오타 교정어를 위한 새
  응답 필드나 화면은 이번 범위에 포함하지 않는다.
- 전달받은 `poudy-full-schema.sql`은 기존 테이블과 검색용 구체화 뷰 3개, 인덱스,
  정규화, 초성, 라틴 읽기, 오타 교정, 검색 순위와 UTF-16 일치 구간 함수를 포함한다.
  구현 후보이며 실행 정확성과 성능이 검증된 것으로 취급하지 않는다.
- 첨부 파일의 현재 위치는 `C:/Users/WM/Downloads/poudy-full-schema.sql`이다.
  구현 시 검증한 SQL을 저장소 스키마에 반영해 개인 경로 의존을 없앤다.
- Excel에서 JSON으로 변환하고 DB에 적재하는 파이프라인은 기존 작업이다.
  `Seonwu-K/poudy-private`의 `db-script`와 `db-load` 구조를 확인했다.
  적재 스크립트 신규 작성, 파이프라인 수정과 초기 데이터 적재는 검색 구현의 선행 작업이 아니다.
  서버는 이미 적재된 테이블과 검색 문서의 계약을 기준으로 구현한다.
- Spring과 PostgreSQL은 로컬에서 실행한다. 테스트 DB는 `poudy_test`다.
  현재 기본 프로필은 `dev`이며 #480의 `local` 표기와 다르므로 실제 설정 기준으로
  실행 문서를 정리한다. Docker 도입과 운영 인프라 구성은 범위에서 제외한다.
- DB 환경 문제는 원인을 확인하고 설치, PATH, 권한 등 근본 해결책을 먼저 제안한다.
  사용자 적용 동의 없이 우회하지 않는다.

## 구현과 검증의 경계

- 구조 파악과 코드 작성은 실제 데이터 적재나 로컬 DB 설치를 기다리지 않는다.
- DB 검색 함수와 저장소의 실행 검증에는 PostgreSQL이 필요하다. `poudy_test`와 저장소의
  테스트 SQL을 사용하고 검색 경계 사례만 테스트 데이터로 추가한다. private 원천 데이터는 필요 없다.
- 각 구현 단계는 관련 테스트까지 통과해야 완료로 표시한다. DB 환경이 없으면 코드 작성은
  진행하되 실행 검증은 미완료로 기록한다. 테스트를 대체하거나 통과한 것으로 취급하지 않는다.
- 실제 카탈로그 적재, 실제 데이터 검색 품질 및 성능 검증은 이 계획에서 제외한다.
  적재 도구와 서버 계약의 불일치는 근거를 기록하며 파이프라인 수정으로 임의 확장하지 않는다.

## 실행 순서와 완료 조건

### 1. 기존 DB 구조와 적재 계약 파악

- [x] private repo 위치와 `db-script`의 지침, `db-load` 실행법을 확인한다.
- [x] 서버 스키마, 첨부 SQL과 적재 스크립트를 비교한다.
- [x] 테이블 매핑, 감각 값 입력과 검색 뷰 갱신 순서를 확인한다.
- [x] 검색 뷰가 현재 서버 스키마에 없으며 피부타입 원천과 큐레이션 데이터가 없는 점을 기록한다.

완료 조건: 서버가 조회할 구조와 기존 적재 도구의 계약을 설명할 수 있다.
구조 확인은 완료했다. DB 설치, 초기 적재, 서버 기동과 테스트 통과는 1단계 조건이 아니다.

### 2. 검색 계약과 기대 결과 정의

- [x] 기존 테스트와 명시적인 테스트 입력으로 정규화, 초성, 영문 읽기, 별칭과 브랜드 조합 사례를 정리한다.
- [x] 전체 일치, 접두, 포함, 전체 토큰과 일부 토큰의 적용 범위를 정리한다.
- [x] SQL의 오타 교정, 최근 30일 조회수, 이름 길이와 동점 처리의 기대 결과를 정의한다.
- [x] 상품 목록의 이름순과 대표 옵션 가격순, 제안의 관련도 순위를 구분한다.
- [x] 성분 제안 최대 5개, 일치 필드와 UTF-16 강조 구간 계약을 보존한다.

완료 조건: 의도한 순위 변경과 회귀를 구분하는 기대 결과가 테스트 사례로 남는다.
기존 순위의 완전 일치는 요구하지 않지만 변경 결과에 근거를 남긴다.
결과는 [PostgreSQL 검색 계약](../../product/postgres-search-contract.md)에 기록했다.
테스트 사례는 문서 명세이며 새 SQL의 실행 테스트 구현과 통과는 3~5단계에서 확인한다.

### 3. 서버 검색 SQL 구현과 테스트 DB 검증

- [ ] 첨부 SQL을 검토해 서버 스키마에 검색 구조를 구현한다.
- [ ] 실행 검증 시 로컬 PostgreSQL, `pg_trgm`, UTF8과 로케일을 확인하고 `poudy_test`를 준비한다.
  필요한 설치나 권한 변경은 이 검증 시점에 확인한다.
- [ ] 테스트 SQL로 검색 함수와 스키마를 실행하고 오류 및 계약 불일치를 수정한다.
- [ ] 신규 DB 생성과 기존 DB의 검색 구조 추가 절차를 각각 마련한다.
- [ ] 빈 질의, 정규화 후 빈 질의, 짧은 질의, 긴 토큰, 한글 조합과 전각 문자를 검증한다.
- [ ] 검색 결과, total, 빈 페이지와 강조 구간을 검증한다.
- [ ] 기존 적재 도구가 호출하는 `product_search_document`, `ingredient_search_term`,
  `search_vocabulary`의 이름과 갱신 순서를 유지한다.
- [ ] 서버 테스트 초기화에서 테스트 데이터 삽입 후 검색 뷰를 갱신한다.

완료 조건: 테스트 데이터로 검색 함수 계약이 검증되고 기존 적재 도구와 호환된다.
private 적재 스크립트 작성이나 수정은 이 단계에 포함하지 않는다.
첨부 SQL 전체를 기존 DB에 그대로 재실행하지 않는다.

### 4. 성분 검색과 조회 전환

- [ ] 한글명, 영문명, 별칭 검색과 제안을 DB 검색 결과에 연결한다.
- [ ] 검색 결과의 순서, 필드와 강조 구간을 기존 API에 매핑한다.
- [ ] 상세, ID 목록, 페이지 조회를 필요한 데이터만 조회하도록 전환한다.
- [ ] 사용 중인 성분 조건, 포함 상품 수와 제외 성분군 조회를 연결한다.

완료 조건: 전체 성분 로딩 없이 성분 API가 동작하며 입력 ID 순서 등 기존 계약을 유지한다.

### 5. 상품 검색, 필터와 집계 전환

- [ ] DB 검색에 상품과 브랜드 조합 및 제안을 연결한다.
- [ ] 카테고리, 브랜드, 피부타입, 수분감, 유분감, 성분 포함과 제외 조건을 구현한다.
- [ ] 목록과 count가 같은 조건을 사용하도록 구현한다.
- [ ] 정렬과 페이지 처리를 DB에서 수행하고 해당 페이지의 관계 데이터만 조회한다.
- [ ] 대표 옵션 가격, 동점 ID 순서와 관계 조인에 따른 중복을 검증한다.
- [ ] 필터 선택지는 해당 축의 필터만 제외하는 기존 집계 규칙을 보존한다.
- [ ] 검색 함수의 전체 JSON 결과를 Java로 가져와 다시 필터링하는 구조를 피한다.
  필터와 결합 가능한 행 결과 또는 쿼리 경계를 검토한다.

완료 조건: 목록, count와 선택지 집계가 일치하며 검색과 필터 적용 뒤 페이지를 자른다.

### 6. 연관 기능의 전체 로딩 의존 제거

- [ ] 브랜드와 카테고리 상품 수, 성분 사용 집계를 DB 조회로 전환한다.
- [ ] 보관 목록과 큐레이션을 필요한 상품 ID 조회로 전환한다.
- [ ] 공유 텍스트 식별의 기존 계약을 유지하며 전용 조회 경계를 마련한다.
- [ ] 조회수 기록과 정정 요청의 상품 존재 확인을 전환한다.
- [ ] 인기 상품 랭킹의 단종 제외와 정렬 규칙을 유지한다.
- [ ] `CatalogKeywordSearch`의 검색 결과 존재 확인을 DB 경계에 연결한다.
- [ ] DB 검색 결과와 기동 시 캐시가 서로 다른 시점의 데이터를 쓰지 않는지 검증한다.

완료 조건: 상품과 성분의 전체 로딩 없이 연관 기능이 동작한다.
작은 사전 데이터의 캐시까지 일괄 제거하지 않고 갱신 정책을 명시한다.

### 7. 도메인, 엔티티와 설정 정리

- [ ] 최종 조회 경계를 기준으로 `Product`, `Ingredient`와 중복 엔티티의 통합을 검토한다.
- [ ] 직접 JPA 매핑 시 전성분의 순서와 중복, 옵션 순서, 태그 근거 규칙을 보존한다.
- [ ] 검색 점수와 강조 구간은 조회 결과 모델로 분리한다.
- [ ] 불필요해진 변환, 전체 로딩 생성자와 메모리 검색 인덱스를 제거한다.
- [ ] `SnapshotReader`, `Products`, `IngredientCatalog`와 Config는 남은 사용처를 확인한다.
  인기 검색어 랭킹 스케줄러 등 필요한 설정은 유지한다.
- [ ] 패키지 순환 금지와 도메인 책임을 유지하고 아키텍처 문서를 갱신한다.

완료 조건: 중복 상태와 전환 전 코드가 정리되고 기능 및 계층 의존 검증을 통과한다.
엔티티 통합은 검색 구현의 기술적 선행 조건이 아니다. 현재 문서는 복합 도메인의 별도
엔티티를 허용하므로 채택한 설계와 문서를 함께 맞춘다.

### 8. 테스트 데이터 기반 회귀 검증과 빌드

- [ ] 로컬 `poudy_test`에서 검색, 저장소와 API 통합 테스트를 실행한다.
- [ ] 테스트 데이터로 짧은 질의, 교정 실패, 복합 필터와 페이지 경계를 검증한다.
- [ ] 테스트 데이터 변경 및 검색 뷰 갱신 후 결과를 검증한다.
- [ ] 테스트에서 N+1 조회와 불필요한 전체 결과 조회가 없는지 확인한다.
- [ ] `sh ./scripts/verify.sh`로 API 생성물 드리프트와 전체 빌드를 검증한다.
- [ ] 서버 스키마와 테스트 초기화 변경, 실행 방법을 문서에 반영한다.

완료 조건: 테스트 데이터로 API 계약과 합의한 검색 동작을 검증하고 전체 빌드를 통과한다.
실제 데이터 검증, 운영 배포와 인프라 변경은 범위에 포함하지 않는다.

## 진행 기록

- 2026-09-20: 2단계 완료. 기존 구현과 첨부 SQL을 비교해 검색 계약과 합성 사례를 작성했다.
  전체 토큰 판정, 브랜드 접두 복합 검색과 초성/읽기 보존 등 SQL 보완 항목을 명시했다.
  기존 검색 도메인 4개 클래스의 테스트 79개가 모두 통과했다.
  서버 구현과 DB 환경은 변경하지 않았으며 DB 통합 검증은 아직 수행하지 않았다.
- 2026-09-20: 사용자 지시에 따라 초기 데이터 적재와 환경 설치를 1단계 완료 조건에서
  제외했다. 기존 구조 파악으로 1단계를 완료하고 코드 구현을 진행한다.
  검증은 `poudy_test`의 테스트 데이터와 전체 빌드로 한정한다.
  추가 사용자 지시에 따라 실제 데이터 적재와 품질 및 성능 검증 계획도 제외했다.
- 2026-09-20: `Seonwu-K/poudy-private`의 `db-script`를 가져와 적재 지침과 입력을 확인했다.
  상품 472개의 감각 값 누락은 없다. 적재기는 서버 스키마에 아직 없는 검색 뷰를 요구한다.
  세부 구조와 검증 이력은 [기존 적재 구조 확인 기록](postgres-local-baseline.md)에 기록했다.
- 2026-09-20: 기준 브랜치의 코드, 첨부 SQL과 #480을 읽고 후속 범위를 정리했다.
  #480에서 검색 순위 변경과 상품 목록의 전체 토큰 일치가 이미 결정되었음을 확인했다.
  SQL 실행, private repo 확인과 환경 설치는 아직 수행하지 않았다.
